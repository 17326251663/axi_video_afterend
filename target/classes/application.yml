server:
  port: 9090
#redis配置
spring:
  redis:
    database:   0
    host: 127.0.0.1
    port: 6379
    #服务名和数据源(连接池)


  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/axi_video?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: rootgkp